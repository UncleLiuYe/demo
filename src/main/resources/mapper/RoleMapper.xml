<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="site.liuye.demo.mapper.RoleMapper">
    <insert id="insertRole" parameterType="site.liuye.demo.domain.Role" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO roles (name)
        VALUES (#{name})
    </insert>
    <update id="updateRole">
        UPDATE roles
        SET name = #{name}
        WHERE id = #{id}
    </update>
    <delete id="deleteRole">
        DELETE
        FROM roles
        WHERE id = #{id}
    </delete>
    <select id="findAll" resultType="site.liuye.demo.domain.Role">
        SELECT *
        FROM roles
    </select>
</mapper>